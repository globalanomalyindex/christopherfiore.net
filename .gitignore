node_modules/
dist/
.DS_Store
*.log

# Source handoff packages. Keep locally for reference; they are large and are
# not part of the build. Remove this line if you want them tracked.
_source/

# Browser-automation debris from build/verification sessions
.playwright-mcp/
/menu.png
/p3-before.png
/page01.png

# Scratch harness for the lattice primitive — dev only, never built (Vite only
# emits index.html), but there is no reason for it to be in the history.
/lattice-check.html
/src/lattice-check.ts
